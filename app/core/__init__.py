"""
This package contains modules related to application configuration.

* database.py: manages database connection with SQLAlchemy,
  creates the database engine and sessions used by the application.

* security.py: contains security functions such as password hashing
  with bcrypt and the creation of JWT tokens for authentication.
"""
